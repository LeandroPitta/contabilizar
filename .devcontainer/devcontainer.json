{
  "name": "Ubuntu + Node + Angular",
  "dockerFile": "Dockerfile",
  "appPort": [4200],
  "forwardPorts": [4200],
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "postCreateCommand": "node --version && ng version",
  "extensions": [
    "vscjava.vscode-java-pack",
    "ms-azuretools.vscode-docker",
    "angular.ng-template"
  ]
}